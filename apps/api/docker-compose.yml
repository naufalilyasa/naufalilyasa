services:
  postgres:
    image: postgres:latest
    container_name: postgres_naufalilyasa
    ports:
      - "6501:5432"
    volumes:
      - postgresDB_naufalilyasa:/data/postgres
    env_file:
      - ./.env

  redis:
    image: redis:alpine
    container_name: redis_naufalilyasa
    ports:
      - "6380:6379"
    volumes:
      - redisDB_naufalilyasa:/data
volumes:
  postgresDB_naufalilyasa:
  redisDB_naufalilyasa:
